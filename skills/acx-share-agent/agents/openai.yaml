interface:
  display_name: "Share ACX Artifacts"
  short_description: "Verify and publish portable agent artifacts"
  default_prompt: "Use $acx-share-agent to verify this ACX artifact and prepare its immutable, PR-only publication to the static Exchange."
